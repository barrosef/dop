meta {
  name: GetTree
  type: grpc
  seq: 1
}

grpc {
  url: {{bffGrpc}}
  method: /dop.bff.v1.HierarchyService/GetTree
  body: grpc
  auth: none
}

metadata {
  authorization: Bearer {{idToken}}
  x-account-id: {{accountId}}
}

body:grpc {
  [
    {}
  ]
}

docs {
  The active account's tree in a single response.

  A project with no board does NOT carry the `task_manager` field (in protobuf,
  absent and zeroed are different things) — the equivalent of REST's `null`.
}
