# Environment variables of DOP's local stack (an example).
# Copy it to infra/.env and fill it in. NEVER commit secrets.

# --- dop-api ---
# DOP_API_PORT=8787
# DOP_DB_URL=postgresql://dop:dop@db:5432/dop

# --- the integration with Claude (decision D1 is open) ---
# ANTHROPIC_API_KEY=
# CLAUDE_INTEGRATION=agent-sdk        # agent-sdk | api | cli (to be defined)

# --- dop-app ---
# DOP_APP_PORT=5180
# VITE_API_BASE_URL=http://localhost:8787
