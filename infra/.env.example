# Variáveis de ambiente do stack local do DOP (exemplo).
# Copie para infra/.env e preencha. NUNCA commitar segredos.

# --- dop-api ---
# DOP_API_PORT=8787
# DOP_DB_URL=postgresql://dop:dop@db:5432/dop

# --- integração com o Claude (decisão D1 em aberto) ---
# ANTHROPIC_API_KEY=
# CLAUDE_INTEGRATION=agent-sdk        # agent-sdk | api | cli (a definir)

# --- dop-app ---
# DOP_APP_PORT=5180
# VITE_API_BASE_URL=http://localhost:8787
