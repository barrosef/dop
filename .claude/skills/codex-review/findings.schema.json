{
  "type": "object",
  "additionalProperties": false,
  "required": ["verdict", "summary", "findings"],
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["clean", "advisory", "blocking"],
      "description": "blocking if any finding breaks an invariant or is a correctness defect; advisory if only lesser findings; clean if none"
    },
    "summary": {
      "type": "string",
      "description": "One sentence stating what was reviewed and the headline result."
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["severity", "invariant", "file", "line", "summary", "why"],
        "properties": {
          "severity": {
            "type": "string",
            "enum": ["blocking", "correctness", "advisory"]
          },
          "invariant": {
            "type": ["string", "null"],
            "description": "Which AGENTS.md invariant this breaks, verbatim, or null."
          },
          "file": { "type": "string" },
          "line": { "type": ["integer", "null"] },
          "summary": { "type": "string" },
          "why": {
            "type": "string",
            "description": "Concrete consequence: inputs or state that produce the wrong result. Not a restatement of the summary."
          }
        }
      }
    }
  }
}
